# Sandbox-only module wiring; never part of a build anyone else runs.
go.work
go.work.sum

# Build output
/pingping
/pingping
/pingping.exe
/dist/

# A portable install writes here.
/data/
*.db
*.db-wal
*.db-shm
